host: 127.0.0.1
port: 3306
database: database
username: username
password: password
useSSL: false
pool:
  name: HyperNiteMC-Pool
  minSize: 5
  maxSize: 10
  connectionTimeout: 30000
  idleTimeout: 60000
  maxLifeTime: 600000
redis:
  enabled: false
  ip: 127.0.0.1
  port: 6379
  timeout: 5 # in sec
  usePassword: false
  password: ""
chatFormatPlaceholder: "%player_name%"
#Chatformat inbuild placeholder
#                <game-stats> , gamestats
#                <prefix> ,chat.getPlayerPrefix(player)
#                <suffix> ,chat.getPlayerSuffix(player)
#                <g-prefix> ,chat.getGroupPrefix(player.getWorld().getName(),primaryGroup)
#                <g-suffix> ,chat.getGroupSuffix(player.getWorld().getName(),primaryGroup)
#                <player>  , player.getDisplayName
#                <message>

